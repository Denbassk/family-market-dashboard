# План патчей дашборда (ревизия кода 2026-09-18)

Диалог с Claude по разбору логики. Каждый патч — отдельный коммит.
Патчеры лежат в tools/patch_*.py, идемпотентны (повторный запуск = SKIP).

## Замеры до правок (docs/full_data.json, прогон 35334532440)

- moves: summary показывал 8677 пар / 1 967 566 грн, в таблицу уходило 300 строк (LIMIT 300)
- moves: 48 доноров из 300 строк отдавали больше остатка (Михайля Семенка 17: 873 из 313)
- oos: 500 строк, elsewhere=true у 295, из них 241 (82%) без реального донора
- oos топ: Вода Імператорська 1л (sold90 31094), Пакет Фемелі S, БІР Кег Пиво — непоштучные товары
- writeoffs_by_store: 20 точек из 38 (причина — LIMIT 20, а не отчётность магазинов)
- dead_total 2 715 477 грн; dead_by_cat «Прочее (нет в матрице)» 994 142 грн / 2325 SKU
- docs/page3_data.json от 2026-08-07 — мёртвый файл, фронт его не читает

## Статус

| # | Патч | Статус | Коммит |
|---|------|--------|--------|
| 1 | moves: жадное распределение вместо декартова произведения | СДЕЛАНО | bc79016 |
| 2 | oos: nonstock -> oos_nonstock, LIMIT 500->1200: bylo 265 realnyh, stalo 841 (+576 ranee obrezannyh); 359 bez ucheta | SDELANO | |
| 11 | oos: filtr nonstock perenesti v SQL do LIMIT, chtoby limit ne tratilsya na musor | OTKRYTO | |
| 3 | writeoffs_by_store: снять LIMIT 20 | СДЕЛАНО | |
| 4 | oos: elsewhere = есть реальный донор (ELSEWHERE_MIN, без отрицательных, без склада) | СДЕЛАНО | |
| 5 | build_data.py: явный encoding=utf-8 во всех open() | СДЕЛАНО | |
| 6 | moves: порог MIN_MOVE_VALUE (сейчас 0 — в плане есть перемещения на 2 грн) | ОТКРЫТО | |
| 7 | dead_items: учитывать дату первого прихода, не считать неликвидом новые поставки | ОТКРЫТО | |
| 8 | cross_items: lift вместо COUNT(DISTINCT tid) + фильтр тары (сейчас топ = пакеты и крышки) | ОТКРЫТО | |
| 9 | unmatched: отдельный отчёт по 2325 SKU вне матрицы (994 тыс. грн неликвида без категории) | ОТКРЫТО | |
| 10 | удалить мёртвый docs/page3_data.json + проверка свежести данных в гейте | ОТКРЫТО | |

## Результат патча 1 (прогон 35334532440)

    moves allocation: pairs 8677 -> 1742, qty 36906 -> 7964, value 1967566 -> 405708, donors_used 1195

Четыре пятых прежнего плана были физически неисполнимы: один остаток раздавался
в среднем пяти получателям (SQL делает donors JOIN recv без учёта бюджета).

## Что важно помнить при правках

- Новые ключи из fetch_page3.py надо добавлять в список мержа в build_data.py,
  иначе они молча не попадут в docs/full_data.json.
- docs/page3_data.json не используется фронтом; данные страницы 3 мержатся в full_data.json.
- Имена точек канонизируются только в scripts/domain.py (NORM). В BigQuery канона нет.
- Гейт tests/check_dashboard.js блокирует коммит при провале — прод остаётся целым.
- Полный прогон сборки читает ~21 ГБ BigQuery, поэтому правки группируем.
## Итог 2026-09-18

- oos: 3000 строк (упирается в лимит, нужен порог qty90>=30 вместо 10) — пункт 12
- oos_nonstock: 400
- writeoffs: 32 точки из 38
- moves: 1742 пары / 405708 грн
- unmatched (пункт 9): отложен, требует barcode в _dash_dead (колонка добавлена)


## Итог 2026-09-18 (финал, прогон после батча 6/8/9/12)

- moves: 843 пары / 342 858 грн (было 8677 / 1 967 566), порог MIN_MOVE_VALUE=150
- oos: 2361 строка (порог OOS_MIN=30, лимит 3000 больше не упирается), nonstock 400
- cross_items: 80 пар разных брендов (lift, CROSS_MIN=50); cross_variants: 40 пар одной линейки
- unmatched: 735 штрих-кодов на 1 141 190 грн — список для заведения в матрицу
- writeoffs: 32 точки из 38 (остальные не сдают отчёт, не баг кода)
- page3_data.json удалён; в CI добавлен шаг проверки f-строк (tools/check_fstrings.py)

Закрыто 9 из 10. Открыт пункт 7: не считать неликвидом свежие поставки (нужна дата
первого прихода, источник — scripts/fetch_incoming.py, отдельная задача).

Грабли, стоившие времени: переменная в f-строке, объявленная ниже использования
(NameError только в рантайме); regexp с \W внутри f-строки (SyntaxError на Python 3.13 —
писать без слэшей); правки по номерам строк вместо поиска по тексту.
