# План патчей дашборда (ревизия кода 2026-09-18)

Диалог с Claude по разбору логики. Каждый патч — отдельный коммит.
Патчеры лежат в tools/patch_*.py, идемпотентны (повторный запуск = SKIP).

## Замеры до правок (docs/full_data.json, прогон 35334532440)

- moves: summary показывал 8677 пар / 1 967 566 грн, в таблицу уходило 300 строк (LIMIT 300)
- moves: 48 доноров из 300 строк отдавали больше остатка (Михайля Семенка 17: 873 из 313)
- oos: 500 строк, elsewhere=true у 295, из них 241 (82%) без реального донора
- oos топ: Вода Імператорська 1л (sold90 31094), Пакет Фемелі S, БІР Кег Пиво — непоштучные товары
- writeoffs_by_store: 20 точек из 38 (причина — LIMIT 20, а не отчётность магазинов)
- dead_total 2 715 477 грн; dead_by_cat «Прочее (нет в матрице)» 994 142 грн / 2325 SKU
- docs/page3_data.json от 2026-08-07 — мёртвый файл, фронт его не читает

## Статус

| # | Патч | Статус | Коммит |
|---|------|--------|--------|
| 1 | moves: жадное распределение вместо декартова произведения | СДЕЛАНО | bc79016 |
| 2 | oos: nonstock -> oos_nonstock, LIMIT 500->1200: bylo 265 realnyh, stalo 841 (+576 ranee obrezannyh); 359 bez ucheta | SDELANO | |
| 11 | oos: filtr nonstock perenesti v SQL do LIMIT, chtoby limit ne tratilsya na musor | OTKRYTO | |
| 3 | writeoffs_by_store: снять LIMIT 20 | СДЕЛАНО | |
| 4 | oos: elsewhere = есть реальный донор (ELSEWHERE_MIN, без отрицательных, без склада) | СДЕЛАНО | |
| 5 | build_data.py: явный encoding=utf-8 во всех open() | СДЕЛАНО | |
| 6 | moves: порог MIN_MOVE_VALUE (сейчас 0 — в плане есть перемещения на 2 грн) | ОТКРЫТО | |
| 7 | dead_items: учитывать дату первого прихода, не считать неликвидом новые поставки | ОТКРЫТО | |
| 8 | cross_items: lift вместо COUNT(DISTINCT tid) + фильтр тары (сейчас топ = пакеты и крышки) | ОТКРЫТО | |
| 9 | unmatched: отдельный отчёт по 2325 SKU вне матрицы (994 тыс. грн неликвида без категории) | ОТКРЫТО | |
| 10 | удалить мёртвый docs/page3_data.json + проверка свежести данных в гейте | ОТКРЫТО | |

## Результат патча 1 (прогон 35334532440)

    moves allocation: pairs 8677 -> 1742, qty 36906 -> 7964, value 1967566 -> 405708, donors_used 1195

Четыре пятых прежнего плана были физически неисполнимы: один остаток раздавался
в среднем пяти получателям (SQL делает donors JOIN recv без учёта бюджета).

## Что важно помнить при правках

- Новые ключи из fetch_page3.py надо добавлять в список мержа в build_data.py,
  иначе они молча не попадут в docs/full_data.json.
- docs/page3_data.json не используется фронтом; данные страницы 3 мержатся в full_data.json.
- Имена точек канонизируются только в scripts/domain.py (NORM). В BigQuery канона нет.
- Гейт tests/check_dashboard.js блокирует коммит при провале — прод остаётся целым.
- Полный прогон сборки читает ~21 ГБ BigQuery, поэтому правки группируем.
## Итог 2026-09-18

- oos: 3000 строк (упирается в лимит, нужен порог qty90>=30 вместо 10) — пункт 12
- oos_nonstock: 400
- writeoffs: 32 точки из 38
- moves: 1742 пары / 405708 грн
- unmatched (пункт 9): отложен, требует barcode в _dash_dead (колонка добавлена)


## Итог 2026-09-18 (финал, прогон после батча 6/8/9/12)

- moves: 843 пары / 342 858 грн (было 8677 / 1 967 566), порог MIN_MOVE_VALUE=150
- oos: 2361 строка (порог OOS_MIN=30, лимит 3000 больше не упирается), nonstock 400
- cross_items: 80 пар разных брендов (lift, CROSS_MIN=50); cross_variants: 40 пар одной линейки
- unmatched: 735 штрих-кодов на 1 141 190 грн — список для заведения в матрицу
- writeoffs: 32 точки из 38 (остальные не сдают отчёт, не баг кода)
- page3_data.json удалён; в CI добавлен шаг проверки f-строк (tools/check_fstrings.py)

Закрыто 9 из 10. Открыт пункт 7: не считать неликвидом свежие поставки (нужна дата
первого прихода, источник — scripts/fetch_incoming.py, отдельная задача).

Грабли, стоившие времени: переменная в f-строке, объявленная ниже использования
(NameError только в рантайме); regexp с \W внутри f-строки (SyntaxError на Python 3.13 —
писать без слэшей); правки по номерам строк вместо поиска по тексту.


## Пункт 8 (кросс-продажи): закрыт 2026-09-19

### Что мерили, прежде чем решать

Прогнали кандидатов топ-800 по обороту, пары с cnt>=30: **10 467 пар**. Распределение
показало, что исходная гипотеза («lift поднимает редкое, cnt — частое, они спорят»)
в этих данных НЕ работает: cnt и lift идут вместе.

    cnt    30–50   6020 пар, медиана lift  1,98
    cnt    50–100  3143 пар, медиана lift  2,49
    cnt   100–300  1136 пар, медиана lift  4,46
    cnt   300–1000  158 пар, медиана lift 35,38
    cnt  1000+       10 пар, медиана lift 121,70

Пары «lift 4 при 5000 чеков», ради которой предлагалась сортировка по cnt, в данных нет
вовсе: максимум cnt по всей выборке — 1731. Зато верхушка по cnt целиком занята
вариантами линеек (Волошкове Поле Сирок, Корм Пан Кот, Монжар Жуйка), поэтому сортировка
по чекам без разделения по типу связи даёт ту же бесполезную картину, что и lift DESC.

### Что сделано

**Один ключ вместо трёх, с типом связи в строке.** `cross_items` теперь содержит поле
`kind`, а `cross_variants` удалён. Причина прозаическая: фронт не рисовал ни
`cross_variants`, ни `cross` — обе таблицы считались в BigQuery и молча оседали в JSON.
Проверка `grep` по index.html: использовался только `cross_items`, и только три колонки.

**Тип связи — по категории и общему префиксу названия, бренд не парсится.**
Бренд первым словом ломался на «Корм Пан Кот» и «Кава Петрівська Слобода» (первое слово —
тип товара), `REGEXP_EXTRACT` отдавал NULL на «Бойчак  Соломка» (два пробела).
Общий префикс в два слова и длиннее ловит линейку без словарей и без regexp.
Поля `brand` и `same_brand` из SQL убраны.

| kind | правило | что это |
|------|---------|---------|
| `cross` | разные категории, обе известны, lift>=2 | связка товарных групп — то, ради чего меняют выкладку |
| `same` | одна категория, префикс <2 слов, lift>=2 | берут вместе внутри группы |
| `variant` | общий префикс >=2 слов, lift>=2 | вкусы одной линейки |
| `subs` | одна категория, lift<1 | берут одно ИЛИ другое |
| `nomatrix` | хотя бы одна позиция вне матрицы | тип определить нельзя |

**lift — фильтр, ранг — по чекам.** Медиана lift у межкатегорийных пар 1,94, поэтому
LIFT_MIN=2 срезает ровно ту половину, где связи нет. Внутри типа сортировка по cnt.
Колонка lift осталась в таблице и сортируется кликом — редкие сильные связки
(«Комо Сир Плавлений + Медоф Горілка Калганоф», lift 43 на 65 чеках) не теряются.

**Низкий lift — отдельный сигнал, раньше терялся полностью.** lift<1 внутри одной
категории = «берут одно ИЛИ другое». 42 пары: Кока-Кола 1,25 / 1,75 / 0,5 между собой
(0,70–0,97), батон Нарізний / На Кефірі 0,46, Оболонська / Моршинська 0,40,
Пепсі / Кока 0,42. Это материал для ротации ассортимента, а не для выкладки.
Ограничение «одна категория» обязательно: батон даёт lift<1 почти со всем подряд, но это
не замена, а разные походы в магазин.

**Осторожно со словом «взаимозамена».** Пары одной категории с ВЫСОКИМ lift —
Фанта+Спрайт 38,1; Монстр+Бьорн 30,1; Окрошка+Олів'є 20,6 — берут ВМЕСТЕ, а не вместо.
Поэтому тип называется `same`, а не `subs`.

**Связки категорий (`cross`) вычищены от тары** тем же `nonstock.sql_exclude`, что и
везде, и получили lift. Без фильтра топ был «Пиво + Тара 85 722».

### Главный ответ: межкатегорийные связки есть, их много, но частота обманывает

Лидеры по чекам — это НЕ связки: «Пиво + Табачные» 58 902 чека при lift **0,85**,
«Безалкогольные + Табачные» 45 351 при 0,76. Обе категории просто самые ходовые в сети.
Настоящие связки видны только по lift:

    2,84  Колбасные + Хлебобулочные          14 472 чека
    2,77  Вяленая рыба / Снеки + Пиво        10 519
    2,64  Орешки + Пиво                      11 683
    2,20  Вяленое мясо + Пиво                11 096
    2,11  Молочные продукты + Хлебобулочные  23 458
    1,92  Пиво + Снеки прочие                15 637

По позициям (kind=cross, топ по чекам):

    1006  lift 16,7  Кулінарія Салат Олів'є + Випічка Сосиска в тісті
     903  lift  3,8  Яйце Вищої Категорії + Батон Нарізний Молочний
     758  lift 13,0  Кулінарія Окрошка + Випічка Сосиска в тісті
     661  lift  5,2  Батон Нарізний + Союз Майонез МакМай 320г
     652  lift  3,7  Волошкове Поле Молоко 2,5% + Батон Нарізний

Связки очевидные (хлеб-молоко-яйца-майонез, пиво-снеки), и это нормальный результат:
ценность вкладки не в экзотике топа, а в возможности проверить гипотезу по конкретному
товару — отсортировать по силе связи и посмотреть, что стоит рядом. Самая небанальная
находка — собственное производство: Олів'є и Сосиска в тісті идут вместе в 1006 чеках
при силе связи 16,7, это сильнее любой «классической» пары в сети.

### Фронт

Вкладка «Аналитика», карточка «Кросс-продажи»: переключатель типа связи, колонки
Товар A / Товар B / Категории / Чеков вместе / Сила связи / Доля A % / Доля B %,
подпись под таблицей объясняет выбранный тип и что означает сила связи.
Ниже — новая карточка «Связки категорий» (40 пар, по умолчанию по силе связи).

### Замеры

    было:  cross_items 80 пар (разные бренды) + cross_variants 40 (мёртвый ключ)
           cross 25 пар категорий с тарой, без lift; кандидатов топ-500
    стало: cross_items 752 строки: cross 400/2511, same 150/340, variant 100/977,
           nomatrix 60/130, subs 42/42; cross 40 пар категорий с lift, без тары
           кандидатов топ-800; вес в full_data.json +140 КБ (1% файла)
    цена:  0,40 ГБ сканов на оба запроса (пары 0,153 + категории 0,247), замерено dry-run
    гейт:  tests/check_dashboard.js 105 -> 115 проверок, 0 провалов

Прочие числа прогона не сдвинулись: перемещения 843 / 342 858 ₴, дефицит 2361,
unmatched 735 SKU / 1 141 190 ₴, списания по 32 точкам.

### Осталось открытым

Пункт 7: не считать неликвидом свежие поставки (нужна дата первого прихода из
scripts/fetch_incoming.py). Пункт 11: перенести фильтр nonstock в SQL до LIMIT.


## Наблюдения владельца по живым данным (2026-09-19), в работу не взяты

### 1. Кулинарный остров — связка подтверждается на восьми парах, а не на одной

Владелец заметил «Олів'є + Сосиска в тісті» (1006 чеков, сила 16,7). Проверка показала,
что это не единичная пара, а поведение двух групп целиком: в списке 8 пар
Кулинария <-> Выпечка на 3030 чеков, сила у всех 10,9–17,6.

    1006  16,7  Салат Олів'є + Сосиска в тісті
     758  13,0  Окрошка + Сосиска в тісті
     449  10,9  Оселедець під шубою + Сосиска в тісті
     263  13,2  Сосиска в тісті + Салат з буряка з часником
     162  17,6  Сосиска в тісті + Салат з курячим філе
     157  14,9  Окрошка + Піца з ковбасками

Конверсия пары-лидера: в 9,8% чеков с оливье есть сосиска, в 8,7% чеков с сосиской —
оливье. То есть выкладывать рядом имеет смысл не два SKU, а две витрины.

### 2. Якорность батона: в голове списка — да, по всему списку — нет

Оценка «батон примерно в половине строк первой вкладки» относится к тому, что видно
сверху: в ТОП-20 по чекам батон в 12 строках из 20. По всем 400 строкам вкладки —
22% с любым батоном, 12% именно с «Нарізним Молочним».

Якорность при этом подтверждается другой метрикой, более чистой, — числом разных связок
на позицию:

     46 связок  Ново-Баварський Батон Нарізний Молочний 400г
     36 связок  Яйце Вищої Категорії 1шт
     16 связок  Цукор Фасований ХЦЗ 1кг
     16 связок  Хліб Ново-Баварський Білий 500г
     14 связок  Союз Майонез МакМай 320гр
     14 связок  Яйце Першої Категорії 1шт

Батон впереди вдвое. Вывод владельца («его отсутствие бьёт не только по хлебу») верен,
и он же объясняет, почему батон надо смотреть в дефиците отдельно от прочих позиций.
Кандидат в метрику: «сколько связок теряется при выпадении позиции».

### 3. Вкладка «Взаимозамена» ранжирована неправильно — пункт 13

Владелец прав: у ЭТОГО типа связи логика обратная остальным — чем слабее связь, тем
ценнее строка, а список сортируется по чекам, как все прочие. Факт по проду:

    сила 0,0–0,6:  6 пар   <- настоящая замена (батон Нарізний/На Кефірі 0,46, Пепсі/Кока 0,48)
    сила 0,6–0,8: 11 пар
    сила 0,8–0,9:  8 пар
    сила 0,9–1,0: 17 пар   <- связи нет вообще, это шум в списке

Сорок процентов вкладки — пары со связью 0,9–1,0, то есть независимые. Правка на две
строки: `SUBS_MAX` 1 -> 0,8 (останется 17 пар) и сортировка этой вкладки по силе связи
по возрастанию, а не по чекам. Отдельно стоит оговорить в подписи, что порог по чекам
(30) для взаимозамены работает против нас: чем реже пара встречается вместе, тем сильнее
сигнал, а порог отсекает именно такие.

### 4. Вкладка «Без категории» — 27% тары, причина та же, что у 735 SKU — пункт 14

Фильтр непоштучных товаров (`scripts/nonstock.py`) работает по КАТЕГОРИИ, а у этих
позиций категории нет — они проходят его насквозь. Факт: из 60 пар вкладки 16 содержат
тару или одноразовую посуду (27%, в верхушке — крышки Якобз 340мл и 175мл, соломка
Агропром). Владелец оценил «почти целиком» — опять же по тому, что видно сверху.

Это тот же разрыв, что и 735 SKU на 1 141 190 ₴ в неликвидах: матрица дырявая.
Заполнение матрицы закрывает обе проблемы разом, и вкладка либо очистится, либо станет
пустой за ненадобностью. Костыль (список штрих-кодов тары мимо матрицы) заводить не
стоит — он замаскирует то, что надо починить в источнике.

### Общее замечание к приёмке

В обоих случаях (батон, тара) оценка «почти всё» относилась к верхушке таблицы, а по
всему списку доля втрое-вчетверо меньше. Это не ошибка владельца: сверху лежит то, что
видно. Но при проверке гипотез по этим вкладкам долю надо считать по всему списку.

| # | Что | Статус |
|---|-----|--------|
| 13 | «Взаимозамена»: порог 0,8 и сортировка по силе по возрастанию | ОТКРЫТО |
| 14 | Тара без категории проходит фильтр nonstock (следствие дырявой матрицы) | ОТКРЫТО |
