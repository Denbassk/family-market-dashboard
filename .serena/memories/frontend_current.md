# Фронтенд — недавние изменения (дополняет mem:frontend_dashboard, mem:frontend_filters)

## Дизайн-система (редизайн 2026-08)
CSS-переменные в :root: один акцент `--acc:#4aa8ff`, тени `--shadow`, радиусы, `--ease/--dur`. Контраст текста поднят до WCAG AA (muted #a9b8cf, faint #7f8ea8). Плавные переходы, `@keyframes` fadeUp/chipIn/popIn, уважение `prefers-reduced-motion`. НЕ использовать `background-attachment:fixed` (тормозило скролл — убрано). Карточки/навигация/кнопки — тени и ховеры. KPI на «Обзоре»: первая карточка (Выручка) — герой (`#ov_kpis .kpi:first-child` grid-column:span 2, крупнее). Служебная кнопка «Обновить данные» приглушена (`.btn.acc` — не яркая).
ECharts: `fmtAxis` компактная ось («50 млн»), splitLine пунктиром, выручка — акцент, прибыль — тонкая мягкая линия. Неполный последний месяц в динамике «Обзора» помечается штриховкой + метка «неполный».

## Инструмент «Товары без движения» (вкладка «Перемещения»)
- Ленивая загрузка stale.json. Свои фильтры: stDays, stStore, stCat, мультиселект поставщика (msStSup/ST_SUP), stQ, галочка «скрыть технические» (S.noOff).
- ПОЛЗУНОК `#stRetail` (класс .sw): по умолчанию «склад скрыт» → прячет ТОЛЬКО `Полевая-Склад` (Полевая-Магазин ОСТАЁТСЯ видимой). Переключение → «показывать склад».
- Таблица рендерит ТОП-500 по заморозке (`const CAP=500`) ради производительности; счётчики и Excel-выгрузка (`EXP['st_tab'].rows`) — по полному набору.
- «нет продаж» = d=999 (нет ни одной продажи за окно поиска STALE_MONTHS).
- Блок TSD-экспорта (window.STALE_F, pick/doXlsx/doTxt) — отдельный второй <script>, правил другой сессией, НЕ трогать без нужды.

## Кнопка «Обновить данные»
confirm-напоминание про выгрузку из Торгсофта (дата снимка D.stock_as_of) → счётчик прогресса → опрос ETag full_data.json → «✓ Готово — обновить страницу».

## Вкладки
Обзор, Отчёты, Аналитика, Позиции, «Дефицит·неликвиды·списания» (KPI «Остатки на: <snapshot>»), Магазины (эффективность+теплокарта; GMROI ещё не добавлен), Перемещения, «Кулинария и выпечка», Матрица.
Проверять index.html: `node --check` извлечённого скрипта + jsdom-харнесс. Файл часто правится и параллельной сессией — перечитывать перед правками.