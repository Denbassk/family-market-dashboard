# Открытые задачи

## СДЕЛАНО И ЗАПУШЕНО
**2026-08-20.** Фаза 1: возвраты поставщикам на «Обзоре» (`mem:feature_returns`), сверка
с эталонами (`mem:reconciliation`), маппинг поставщиков в приходах (5,9% -> 97,8%).
Фаза 2: точные кросс-фильтры (`mem:crossfilters_and_cubes`) — куб `prod_store`.
Фаза 3: средний чек без опта (`mem:domain_rules`), автотесты + `tests/truth.py`.
**2026-08-21.** Ревизия вкладок 10 -> 8 (`mem:tabs_review`, `docs/TABS_REVIEW.md`);
`scripts/domain.py` — единый источник имён точек (`mem:domain_stores`).
**2026-08-28.** Фичи: GMROI, дефицит в гривнах, «продаётся у соседей, но не у нас»
(`mem:features_done_2026_08_28`); `tests/check_syntax.js`; исправлены поломки редизайна.
**2026-09-03, коммиты e62d536 и c3eac99 (запушены):**
- цена BigQuery: сборка 21,13 -> 5,98 ГБ, переписаны `fetch_data.py` и `fetch_page3.py`
  на витрины `_dash_a26` / `_dash_r26` / `_dash_s90` / `_dash_stk` / `_dash_dead` —
  `mem:bigquery_cost`;
- CI: гейт автотестов + `git add docs/stale.json` — `mem:ci_gate_and_stale_bug`;
- «Аналитика»: матрица продуктивности запаса вместо обрезанных баров GMROI, вторая
  матрица по магазинам, убраны подписи со скэттера — `mem:analytics_charts`;
- кросс-фильтры: алерты «Обзора» под срез, `catAgg()` перестал терять ABC, замороженный
  капитал по магазинам под срез, бейджи охвата там, где разреза нет —
  `mem:crossfilters_scope_2026_09_03`, инструмент `tests/check_crossfilter.js`.

## ОЧЕРЕДЬ РАБОТ
1. **Партиционировать `turnover_transactions`** — минус ~1,5 ГБ на сборку и минус ~8 ГБ/день
   на стороннем приложении. Риск загрузчика описан в `mem:bigquery_cost`. Ждёт решения владельца.
2. **Удалить `turnover_bak_rename_20260818`** (2,67 ГБ, хранение 8,23 из 10 бесплатных).
   Безопасность проверена, команда отдана владельцу — удаляет он сам.
3. **Расхардкодить YEAR** — мина: 01.01.2027 дашборд тихо покажет нули. Полдня.
   YoY отдельной работой позже (данные за 2025 в `turnover_transactions` есть).
4. **Контракт данных** (вторая половина фазы A): `build_data.py` падает при отсутствии
   обязательного ключа, `data_meta` с версией схемы и контрольными суммами, фронт сверяет.
5. **Карточка поставщика** — по решению владельца в самом конце: условия живут в другом
   проекте на Supabase. См. `mem:feature_suppliers_tab`.

## Аналитика по магазинам — предложено, владелец выбирает
Сделана матрица «оборот x маржа x GMROI». Не сделаны: профиль дня точки против сети
на тепловой карте; динамика доли рынка магазина (растёт точка или растёт вся сеть);
сравнение с 3-5 похожими точками вместо всей сети. Данные на всё это уже есть.

## Технический долг
- Убрать из `fetch_data.py` ключ `culinary` — фронт его больше не читает.
- 40% суммы возвратов (1,35 млн ₴) в «Прочее (нет в матрице)»: возвращают то, что выводят
  из ассортимента, штрих-кодов в матрице уже нет. Сопоставление по названию даёт +3,6 тыс ₴.
- Заполнить `EXCHANGE_SUPPLIERS` в `scripts/returns_config.py` (физобмен) — нужен ответ
  закупок. Подозреваемые: Роганський МК, УДК.
- `store_canon` в BigQuery расходится с NORM в 14 случаях из 42 — пока читать её нельзя,
  фаза B ждёт починки таблицы. Замеры — `mem:domain_stores`.
- Недетерминированный `ANY_VALUE(x HAVING MAX w)` в staging и порядок `D.products`
  (индексы куба съезжают между сборками; на цифры не влияет — сравнивать по именам).
- `turnover_transactions.store_canon` / `store_kind` — целиком NULL, мёртвые колонки.
- Приходы: `in_store_month` из эталона (накладные) против `in_supplier_month` /
  `in_cat_month` из staging (позиции) — расхождение ~1%, в интерфейсе не подписано.
- Выручка по ПРЕЙСКУРАНТУ: скидки 0,37% (357 тыс ₴ за Q1) не учтены. У Торгсофта есть
  эталон продаж только за январь-март, попросить апрель-август.

## Оптимизация
- `full_data.json` 9,8 МБ, `stale.json` 12,9 МБ. Убираемо ~1,4 МБ — `mem:architecture_review` п. 4.
- «Позиции» рендерят 4 тыс строк через innerHTML: узкое место не расчёт, а DOM.
